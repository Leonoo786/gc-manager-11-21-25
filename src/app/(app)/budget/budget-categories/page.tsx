'use client';

export default function BudgetCategoriesPage() {
  return (
    <div style={{ padding: 16 }}>
      <h1>Budget Categories – SIMPLE (nested)</h1>
      <p>
        If you see this on <code>/budget/budget-categories</code>, this page is now
        using the simple version with <strong>no searchTerm</strong> and{' '}
        <strong>no localeCompare</strong>.
      </p>
    </div>
  );
}
